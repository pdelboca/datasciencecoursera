df <- data.frame(id = numeric(),nobs = numeric())
for (i in id) {
file_dir <- paste(directory,file_list[i],sep="")
file_data <- read.csv(file_dir)
id_file = i
print(id_file)
nobs = sum(complete.cases(file_data))
print(nobs)
df <- rbind(df, data.frame(id_file,nobs))
}
as.data.frame(df)
df
}
complete("specdata", 1)
complete <- function(directory, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
directory <- paste(getwd(),"/",directory,"/",sep="")
## Return a data frame of the form:
## id nobs
## 1  117
## 2  1041
## ...
## where 'id' is the monitor ID number and 'nobs' is the
## number of complete cases
file_list <- list.files(directory)
df <- data.frame(id = numeric(),nobs = numeric())
for (i in id) {
file_dir <- paste(directory,file_list[i],sep="")
file_data <- read.csv(file_dir)
id_file = i
print(id_file)
nobs = sum(complete.cases(file_data))
print(nobs)
df <- rbind(df, data.frame(id_file,nobs))
}
as.data.frame(df)
typeof(df)
df
}
complete("specdata", 1)
complete <- function(directory, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
directory <- paste(getwd(),"/",directory,"/",sep="")
## Return a data frame of the form:
## id nobs
## 1  117
## 2  1041
## ...
## where 'id' is the monitor ID number and 'nobs' is the
## number of complete cases
file_list <- list.files(directory)
df <- data.frame(id = numeric(),nobs = numeric())
for (i in id) {
file_dir <- paste(directory,file_list[i],sep="")
file_data <- read.csv(file_dir)
id_file = i
print(id_file)
nobs = sum(complete.cases(file_data))
print(nobs)
df <- rbind(df, data.frame(id_file,nobs))
}
as.data.frame(df)
print(typeof(df))
df
}
complete("specdata", 1)
complete <- function(directory, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
directory <- paste(getwd(),"/",directory,"/",sep="")
## Return a data frame of the form:
## id nobs
## 1  117
## 2  1041
## ...
## where 'id' is the monitor ID number and 'nobs' is the
## number of complete cases
file_list <- list.files(directory)
df <- data.frame(id = numeric(),nobs = numeric())
for (i in id) {
file_dir <- paste(directory,file_list[i],sep="")
file_data <- read.csv(file_dir)
id_file = i
print(id_file)
nobs = sum(complete.cases(file_data))
print(nobs)
df <- rbind(df, data.frame(id_file,nobs))
}
df <- as.data.frame(df)
print(typeof(df))
df
}
complete("specdata", 1)
df <- as.data.frame(df)
complete("specdata", c(2, 4, 8, 10, 12))
complete <- function(directory, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
directory <- paste(getwd(),"/",directory,"/",sep="")
## Return a data frame of the form:
## id nobs
## 1  117
## 2  1041
## ...
## where 'id' is the monitor ID number and 'nobs' is the
## number of complete cases
file_list <- list.files(directory)
df <- data.frame(id = numeric(),nobs = numeric())
for (i in id) {
file_dir <- paste(directory,file_list[i],sep="")
file_data <- read.csv(file_dir)
id_file = i
print(id_file)
nobs = sum(complete.cases(file_data))
print(nobs)
df <- rbind(df, data.frame(id= id_file,nobs = nobs))
}
df <- as.data.frame(df)
print(typeof(df))
df
}
complete("specdata", c(2, 4, 8, 10, 12))
complete <- function(directory, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
directory <- paste(getwd(),"/",directory,"/",sep="")
## Return a data frame of the form:
## id nobs
## 1  117
## 2  1041
## ...
## where 'id' is the monitor ID number and 'nobs' is the
## number of complete cases
file_list <- list.files(directory)
df <- data.frame(id = numeric(),nobs = numeric())
for (i in id) {
file_dir <- paste(directory,file_list[i],sep="")
file_data <- read.csv(file_dir)
id_file = i
print(id_file)
nobs = sum(complete.cases(file_data))
print(nobs)
df <- rbind(df, data.frame(id= id_file,nobs = nobs))
}
df <- data.frame(df)
}
complete("specdata", c(2, 4, 8, 10, 12))
test1 <- complete("specdata", 1)
test1 <- complete("specdata", 1)
str(test1)
test2 <- complete("specdata", c(2, 4, 8, 10, 12))
str(test2)
complete <- function(directory, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
directory <- paste(getwd(),"/",directory,"/",sep="")
## Return a data frame of the form:
## id nobs
## 1  117
## 2  1041
## ...
## where 'id' is the monitor ID number and 'nobs' is the
## number of complete cases
file_list <- list.files(directory)
df <- data.frame(id = numeric(),nobs = numeric())
for (i in id) {
file_dir <- paste(directory,file_list[i],sep="")
file_data <- read.csv(file_dir)
id_file = i
nobs = sum(complete.cases(file_data))
df <- rbind(df, data.frame(id= id_file,nobs = nobs))
}
df <- data.frame(df)
}
submit()
test1 <- complete("specdata", 1)
test1
str(test1)
test2 <- complete("specdata", c(2, 4, 8, 10, 12))
test2
str(test2)
complete <- function(directory, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
directory <- paste(getwd(),"/",directory,"/",sep="")
## Return a data frame of the form:
## id nobs
## 1  117
## 2  1041
## ...
## where 'id' is the monitor ID number and 'nobs' is the
## number of complete cases
file_list <- list.files(directory)
ids <- vector()
nobs <- vector()
for (i in id) {
file_dir <- paste(directory,file_list[i],sep="")
file_data <- read.csv(file_dir)
ids = c(ids,i)
nobs = c(nobs,sum(complete.cases(file_data)))
}
data.frame(id = ids, nobs = nobs)
}
test1 <- complete("specdata", 1)
test1
test2 <- complete("specdata", c(2, 4, 8, 10, 12))
##   id nobs
test2
submit()
getwd()
source('submitscript1.R')
submit
submit()
submit()
directory <- paste(getwd(),"/","specdata","/",sep="")
directory
setwd("/tmp/")
source("submitscript1.R")
source("submitscript1.R")
submit()
6
submit()
submit()
setwd("/home/pdelboca/Repos/datasciencecoursera/R Programming/")
directory <- "specdata"
directory <- paste(getwd(),"/",directory,"/",sep="")
complete_observations <- complete(directory)
source("complete.R")
complete_observations <- complete(directory)
complete_observations$id == 2
complete_observations$nobs > 2
filtered_observations <- complete_observations[complete_observations$nobs > threshold]
filtered_observations <- complete_observations[[complete_observations$nobs > threshold]]
filter = complete_observations$nobs > threshold
threshold = 0
filter = complete_observations$nobs > threshold
filtered_observations <- complete_observations[[filter]]
filtered_observations = subset(observations,observations$nobs > threshold
)
observations <- complete(directory)
filtered_observations = subset(observations,observations$nobs > threshold)
observations <- complete(directory)
source("complete.R")
observations <- complete(directory)
threshold = 50
filtered_observations = subset(observations,observations$nobs > threshold)
filtered_observations
threshold = 350
filtered_observations = subset(observations,observations$nobs > threshold)
file_list <- list.files(directory)
directory <- paste(getwd(),"/",directory,"/",sep="")
directory <- "specdata"
directory <- paste(getwd(),"/",directory,"/",sep="")
file_list <- list.files(directory)
file_dir <- paste(directory,file_list[1],sep="")
file_data <- read.csv(file_dir)
str(file_data)
corr <- function(directory, threshold = 0) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
directory <- paste(getwd(),"/",directory,"/",sep="")
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
observations <- complete(directory)
threshold = 350
filtered_observations = subset(observations,observations$nobs > threshold)
file_list <- list.files(directory)
nitrate <- vector()
sulfate <- vector()
for (i in filtered_observations$id) {
file_dir <- paste(directory,file_list[i],sep="")
file_data <- read.csv(file_dir)
nitrate <- c(nitrate,file_data$nitrate)
sulfate <- c(sulfate,file_data$sulfate)
}
dim(nitrate)
str(nitrate)
dim(sulfate)
str(sulfate)
# duration = faithful$eruptions   # the eruption durations
# waiting = faithful$waiting      # the waiting period
# cor(duration, waiting)          # apply the cor function
## Return a numeric vector of correlations
}
cr <- corr("specdata", 150)
corr <- function(directory, threshold = 0) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
directory <- paste(getwd(),"/",directory,"/",sep="")
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
observations <- complete(directory)
threshold = 350
filtered_observations = subset(observations,observations$nobs > threshold)
file_list <- list.files(directory)
nitrate <- vector()
sulfate <- vector()
for (i in filtered_observations$id) {
file_dir <- paste(directory,file_list[i],sep="")
file_data <- read.csv(file_dir)
file_data <- subset(file_data,complete.cases(file_data))
nitrate <- c(nitrate,file_data$nitrate)
sulfate <- c(sulfate,file_data$sulfate)
}
dim(nitrate)
str(nitrate)
dim(sulfate)
str(sulfate)
# duration = faithful$eruptions   # the eruption durations
# waiting = faithful$waiting      # the waiting period
# cor(duration, waiting)          # apply the cor function
## Return a numeric vector of correlations
}
cr <- corr("specdata", 400)
corr <- function(directory, threshold = 0) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
directory <- paste(getwd(),"/",directory,"/",sep="")
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
observations <- complete(directory)
threshold = 350
filtered_observations = subset(observations,observations$nobs > threshold)
file_list <- list.files(directory)
nitrate <- vector()
sulfate <- vector()
for (i in filtered_observations$id) {
file_dir <- paste(directory,file_list[i],sep="")
file_data <- read.csv(file_dir)
file_data <- subset(file_data,complete.cases(file_data))
nitrate <- c(nitrate,file_data$nitrate)
sulfate <- c(sulfate,file_data$sulfate)
}
cor(nitrate,sulfate)
# duration = faithful$eruptions   # the eruption durations
# waiting = faithful$waiting      # the waiting period
# cor(duration, waiting)          # apply the cor function
## Return a numeric vector of correlations
}
cr <- corr("specdata", 400)
head(cr)
corr <- function(directory, threshold = 0) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
directory <- paste(getwd(),"/",directory,"/",sep="")
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
observations <- complete(directory)
threshold = 350
filtered_observations = subset(observations,observations$nobs > threshold)
file_list <- list.files(directory)
nitrate <- vector()
sulfate <- vector()
correlation <- vector()
for (i in filtered_observations$id) {
file_dir <- paste(directory,file_list[i],sep="")
file_data <- read.csv(file_dir)
file_data <- subset(file_data,complete.cases(file_data))
nitrate <- c(nitrate,file_data$nitrate)
sulfate <- c(sulfate,file_data$sulfate)
correlation <- c(correlation,cor(nitrate,sulfate))
}
correlation
# duration = faithful$eruptions   # the eruption durations
# waiting = faithful$waiting      # the waiting period
# cor(duration, waiting)          # apply the cor function
## Return a numeric vector of correlations
}
cr <- corr("specdata", 400)
head(cr)
corr <- function(directory, threshold = 0) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
directory <- paste(getwd(),"/",directory,"/",sep="")
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
observations <- complete(directory)
filtered_observations = subset(observations,observations$nobs > threshold)
file_list <- list.files(directory)
nitrate <- vector()
sulfate <- vector()
correlation <- vector()
for (i in filtered_observations$id) {
file_dir <- paste(directory,file_list[i],sep="")
file_data <- read.csv(file_dir)
file_data <- subset(file_data,complete.cases(file_data))
nitrate <- c(nitrate,file_data$nitrate)
sulfate <- c(sulfate,file_data$sulfate)
correlation <- c(correlation,cor(nitrate,sulfate))
}
correlation
# duration = faithful$eruptions   # the eruption durations
# waiting = faithful$waiting      # the waiting period
# cor(duration, waiting)          # apply the cor function
## Return a numeric vector of correlations
}
cr <- corr("specdata", 400)
head(cr)
setwd("/home/pdelboca/Repos/datasciencecoursera/R Programming/")
directory <- "specdata"
source("complete.R")
directory <- paste(getwd(),"/",directory,"/",sep="")
observations <- complete(directory)
head(observations)
threshold = 150
filtered_observations = subset(observations,observations$nobs > threshold)
head(filtered_observations)
filtered_observations$id
for (i in filtered_observations$id) {
print(i)
}
for (i in filtered_observations$id) {
file_dir <- paste(directory,file_list[i],sep="")
print(file_dir)
}
file_list <- list.files(directory)
for (i in filtered_observations$id) {
file_dir <- paste(directory,file_list[i],sep="")
print(file_dir)
}
file_data <- read.csv(file_dir)
str(file_data)
file_data <- subset(file_data,complete.cases(file_data))
str(file_data)
cor(file_data$nitrate,file_data$sulfate)
file_data <- read.csv(file_dir)
cor(file_data$nitrate,file_data$sulfate)
corr <- function(directory, threshold = 0) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
directory <- paste(getwd(),"/",directory,"/",sep="")
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
observations <- complete(directory)
threshold = 150
filtered_observations = subset(observations,observations$nobs > threshold)
file_list <- list.files(directory)
nitrate <- vector()
sulfate <- vector()
correlation <- vector()
for (i in filtered_observations$id) {
file_dir <- paste(directory,file_list[i],sep="")
file_data <- read.csv(file_dir)
file_data <- subset(file_data,complete.cases(file_data))
correlation <- c(correlation,cor(file_data$nitrate,file_data$sulfate))
}
correlation
# duration = faithful$eruptions   # the eruption durations
# waiting = faithful$waiting      # the waiting period
# cor(duration, waiting)          # apply the cor function
## Return a numeric vector of correlations
}
corr <- function(directory, threshold = 0) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
directory <- paste(getwd(),"/",directory,"/",sep="")
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
observations <- complete(directory)
filtered_observations = subset(observations,observations$nobs > threshold)
file_list <- list.files(directory)
nitrate <- vector()
sulfate <- vector()
correlation <- vector()
for (i in filtered_observations$id) {
file_dir <- paste(directory,file_list[i],sep="")
file_data <- read.csv(file_dir)
file_data <- subset(file_data,complete.cases(file_data))
correlation <- c(correlation,cor(file_data$nitrate,file_data$sulfate))
}
correlation
# duration = faithful$eruptions   # the eruption durations
# waiting = faithful$waiting      # the waiting period
# cor(duration, waiting)          # apply the cor function
## Return a numeric vector of correlations
}
cr <- corr("specdata", 150)
head(cr)
source("submitscript1.R")
submit()
submit()
submit()
